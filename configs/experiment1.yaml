experiment_name: exp1_smoke

dataset:
  dataset_dir: data/generated/exp1_smoke
  sr: 16000

features:
  n_fft: 1024
  hop_length: 256
  n_mels: 64

training:
  batch_size: 32
  epochs: 30
  lr: 0.001
  num_workers: 0
  audio_eval_every: 5
  audio_eval_n_examples: 20
  seed: 0
